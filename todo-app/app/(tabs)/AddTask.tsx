import {View, Text} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

export default function AddTask() {
    return (
        <SafeAreaView>
            <Text>Edit app/(tabs)/addTask.tsx to edit this screen.</Text>
        </SafeAreaView>
    )
}
